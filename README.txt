This is a simple app where a user can list their favorite bands/artists. It is using mock data for now.
A user can add a new band, edit the name of an exisiting band on the list, or delete a band entirely.
